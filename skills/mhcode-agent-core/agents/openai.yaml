interface:
  display_name: "MHcode Agent 核心"
  short_description: "修改 MHcode Agent 内核、上下文与编排实现"
  default_prompt: "Use $mhcode-agent-core to review this MHcode Agent change and verify permissions, context, terminal states, and recovery."

policy:
  allow_implicit_invocation: true
