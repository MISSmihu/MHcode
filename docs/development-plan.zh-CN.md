# MHcode 开发状态与发布门槛

更新时间：2026-07-20。

本文档描述当前源码已经具备什么、哪些只在测试中验证、发布前还缺什么。它替代早期“先做 DeepSeek MVP”的计划；DeepSeek MVP 已经扩展为多协议 Agent 工作台，后续工作以稳定性和安全边界为主。

## 状态总览

| 能力 | 当前状态 | 说明 |
| --- | --- | --- |
| Wails + Go + SolidJS 工作台 | 已实现 | Windows 构建与桌面启动已验证 |
| DeepSeek 官方流式协议 | 已实现 | 含模型探测、错误处理、用量和工具循环 |
| OpenAI 兼容协议 | 已实现 | 支持自定义地址、请求头和额外 JSON |
| Anthropic 原生协议 | 已实现 | 含工具循环测试 |
| Gemini 原生协议 | 已实现 | 含模型上下文上游字段和工具循环测试 |
| 自定义供应商与模型 | 已实现 | 配置持久化、模型刷新、删除和上下文覆盖 |
| Skills 索引与按需加载 | 已实现 | 内置 `mhcode-agent-core` |
| MCP server 与 schema 快照 | 已实现 | 内置工具和远程工具统一进入注册表 |
| 结构化文件工具 | 已实现 | 文件操作不依赖 Shell |
| Plan 模式 | 已实现 | 只读探索、审批、进度状态和失败收尾 |
| AI 团队模式 | 已实现 | 规划/实现/测试/审阅/汇总角色，含脚本化测试 |
| 上下文自动压缩 | 已实现 | 按模型窗口和推理策略计算预算 |
| 项目记忆、checkpoint、rewind、分叉 | 已实现 | append-only 事件树保留原始历史 |
| Git 工作区 | 已实现 | status/diff/review/stage/commit/branch/worktree |
| 持久终端 | 已实现 | 实时输出、停止、进程树清理 |
| 内置浏览器 | 已实现 | CDP 驱动，依赖本机 Edge/Chrome |
| 电脑/窗口操控 | Windows 优先 | 策略与审批接入，跨平台能力有限 |
| OS 级沙箱 | 部分实现 | Windows Job Object/资源限制；无文件系统或网络隔离 |
| 真实供应商长期压测 | 未完成 | 需要不提交到仓库的测试密钥和可重复基准 |
| 安装包签名、升级和迁移 | 未完成 | 目前以源码构建为主 |

## 已完成的核心闭环

当前默认执行链是：

``text
模型路由
  → 稳定上下文前缀 + MCP/Skills 索引
  → 模型窗口预算与自动压缩
  → Plan（可选）
  → 权限审批
  → 结构化工具循环
  → 流式响应
  → 用量/缓存/事件日志
  → UI 结构化渲染与可 rewind 历史
``

开发新功能时不要绕过这条链直接在前端拼“看起来能用”的状态。尤其不要把计划、工具调用或压缩事件伪装成助手文本。

## 发布前 P0 门槛

以下项目完成前，不应宣称稳定发行版：

1. **安全审计**
   - 明确展示 Windows Job Object 不等于文件系统/网络隔离。
   - 检查所有结构化工具的路径穿越、符号链接、额外可写目录和破坏性操作策略。
   - 检查 MCP 环境变量、HTTP headers、浏览器凭据和供应商密钥不会进入日志或普通配置。
2. **真实协议验证**
   - 为 DeepSeek、Anthropic、Gemini、至少一个 OpenAI 兼容供应商建立脱敏 E2E 测试矩阵。
   - 覆盖连接超时、流中断、空响应、限流、余额不足、工具调用半失败和模型列表异常。
   - 测试密钥只通过 CI secret 或本地环境注入，禁止提交。
3. **桌面发布**
   - Windows 安装、升级、卸载和旧 `runtime-settings.json`/SQLite 迁移。
   - WebView2、Edge/Chrome 不存在或 Crashpad 异常时的可恢复提示。
   - 长任务、浏览器帧刷新和终端输出的内存/帧率检查。
4. **可复现检查**
   - 前端 typecheck、前端回归测试、Go 全测、`go vet`、Wails clean build。
   - 在干净目录从 clone 到启动走一遍，不依赖开发机已有配置。

## 发布前 P1 门槛

- 把 Go 与前端模型上下文目录合并成单一生成源。
- 增加 CI：Windows 主检查、依赖锁文件检查、秘密扫描和构建产物检查。
- 完善多窗口/系统 DPI、输入法、键盘导航和屏幕阅读器测试。
- 为浏览器原生表面增加高 DPI、窗口最小化、设置页切换和输入法回归测试。
- 增加导入/导出运行设置的版本化格式，不导出明文密钥。
- 对 AI 团队增加取消、单角色超时、供应商故障转移和费用上限。
- 对持久终端增加会话恢复策略和退出时残留进程清理验证。

## P2 方向

- Linux/macOS 的进程限制和浏览器原生表面补齐。
- 可选的真正 OS 沙箱后端（Windows AppContainer、容器或受控 helper），并在 UI 中区分“策略沙箱”和“OS 隔离”。
- 供应商模型目录的远端缓存、过期策略和人工校正记录。
- 更细的成本预算、缓存命中诊断和批量基准工具。
- 插件/Skill 市场与签名校验。

## 修改一项能力的验收模板

1. 定义 Go 类型、权限边界和持久化行为。
2. 写服务层单元测试，覆盖成功、拒绝、取消和重启恢复。
3. 把事件加入统一 `ChatStreamEvent`，避免额外私有通道。
4. 同步前端类型、加载态、失败态和空态。
5. 检查稳定前缀是否被新字段或不稳定 schema 打乱。
6. 跑完整本地检查并记录平台限制。
7. 更新本文件和相关 Skill，说明“已实现”还是“仅 mock/测试验证”。

## 当前建议顺序

1. 先完成 P0 安全与真实协议矩阵。
2. 再补 CI 和单一模型上下文目录。
3. 然后做桌面性能、输入法、DPI 与浏览器稳定性。
4. 最后扩展更多供应商、插件和跨平台沙箱。

`go test` 全部通过不等于 Agent 已经在真实供应商、真实浏览器和真实用户工作区中达到发布质量；文档必须区分这三种验证层级。
