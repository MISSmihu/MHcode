# MHcode v0.3.20

## MCP 视觉桥接

- 文本模型收到图片时，可以调用用户配置的 MCP 视觉工具获取文字或结构化分析。
- MCP 设置支持选择视觉工具，并配置图片参数、提示词参数、Data URL/Base64、MIME 类型参数和文件名参数。
- 远程 Streamable HTTP/SSE MCP 的图片上传必须单独授权；未授权时不会发送图片内容。
- MCP 返回的图片内容和嵌入图片资源会保留为真实附件，不再只显示一个摘要字符串。
- 图片的视觉分析结果写入会话附件记录，切换会话、重启应用或上下文恢复后仍可使用。

## Windows 停止可靠性

- `exec.CommandContext` 的取消回调现在通过 Windows Job Object 终止整个命令树，而不是只结束根进程。
- 增加进程尚未完成 Job Object 绑定时的取消竞态保护。
- 增加应用级和底层 Windows 子进程树回归测试。

## 模型目录与 DeepSeek 余额

- DeepSeek V4 Flash 与 V4 Pro 的上下文窗口统一按官方目录显示为 `1,000,000 tokens`。
- 旧版本保存的 `128,000` 供应商默认值会在加载和刷新模型时重新计算为精确目录值；DeepSeek Chat 与 Reasoner 仍保持各自的 128K 目录数据。
- DeepSeek 官方连接测试会调用 `/user/balance`，设置页显示总余额、充值余额、赠送余额、币种和账户可用状态。
- 余额接口异常只显示余额查询错误，不会覆盖模型列表的连接成功状态；余额刷新使用独立超时，并拒绝向与 API 地址不同源的余额 URL 发送 API Key。

## 验证

- `go test ./... -count=1`
- `go vet ./...`
- 前端 104 项测试、TypeScript 检查、Vite 生产构建
- Wails Windows amd64 生产构建
- Windows 命令树停止专项测试连续 100 次通过
