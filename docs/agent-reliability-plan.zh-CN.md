# MHcode Agent 可靠性整改计划

更新时间：2026-07-27

本文档是 Agent 底层可靠性的长期执行清单。后续开发、对话压缩或任务恢复时，应先读取本文档，再根据代码和测试更新状态。不得仅因为单元测试通过就把部分实现标记为完成。

## 状态定义

- `未开始`：尚无可用实现。
- `进行中`：已有代码，但关键验收条件未满足。
- `待验证`：实现已完成，尚缺完整回归或真实环境验证。
- `已完成`：实现、回归测试和要求中的异常路径均已验证。
- `阻塞`：存在明确外部依赖，当前无法继续。

## 当前总览

| 优先级 | 工作项 | 状态 | 当前结论 |
| --- | --- | --- | --- |
| P0 | 任务超时、快速停止与终态管理 | 待人工验收 | 全工具、审批、MCP、插件、子代理和迟到事件自动化已通过；应用级停止 E2E 已验证 Windows 进程树和唯一终态，仍需发布版 UI 与不可撤销远端副作用边界验收 |
| P0 | 重复失败识别与策略切换 | 已完成 | 分支级失败状态、执行前拦截、错误分类、结构化替代建议及停滞 E2E 已通过 |
| P0 | Windows Shell 参数与结构化进程启动 | 待人工验收 | 全量 Go、前端、Windows 专项回归及 Wails 生产构建均通过；仅保留发布版 GUI 长命令停止验收 |
| P0 | 项目解除挂载与重新恢复 | 已完成 | 同一路径恢复原项目 ID、最后活动会话、当前分支、计划、团队中断状态和长期记忆；已覆盖解除挂载期间重启 |
| P0 | 稳定项目、会话和分支身份 | 待验证 | 代码关系已稳定；仍需真实上游缓存重新挂载测试 |
| P0 | 会话级产物登记表 | 已完成 | 产物更新进入 append-only 分支事件；完整元数据、工具归属、验证状态、重启/切换/rewind/分叉恢复及模型私有上下文均已回归验证 |
| P0 | Anthropic 新模型能力、推理字段与上下文元数据 | 待真实上游验收 | Claude 5/新 4.x 能力矩阵、合法 thinking/effort、采样字段省略、128K 输出上限、上游能力持久化和精确上下文目录已通过自动化；仍需官方及中转真实请求验收兼容错误学习 |
| P0 | 本机诊断路由与工具结果最终综合 | 已完成 | 搜索只作为发现线索；空正文会继续调查并进行一次禁用新工具的最终综合，二次空正文失败；本机 Claude/CCSwitch 诊断优先真实本机状态，相关回归已通过 |
| P1 | 通用视觉验证闭环 | 待人工验收 | Edge 后台真实渲染及 DOCX/XLSX/PPTX/HTML/PDF 端到端验收已通过，源文件 SHA 不变且不污染标签页；仍需发布版界面、桌面窗口和真实视觉上游验收 |
| P1 | Codex 式实时工作时间线 | 待人工验收 | 结构化运行快照、崩溃中断恢复、跨会话活动恢复及自然语言进展均已实现并通过自动化；仍需发布版真实长任务视觉与交互验收 |
| P1 | Agent 上下文与编排审计 | 已完成 | 动态上下文已进入真实请求；System Prompt 冲突、错误工具降级、MCP/Plan 工具继承及中断长任务重启继续均已回归验证 |
| P1 | Agent 结构化拉取与下载 | 待人工验收 | `download_file` 与 `git_repository` 已进入主 Agent、团队实现者和子代理；D/E/F 盘中文空格路径的真实下载、clone 和 pull 已通过，仍需发布版动态官网、大文件断网及重启续用验收 |

## P0-1：任务超时、快速停止与终态管理

### 已实现

- 上游模型建连超时为 45 秒。
- 上游模型流空闲超时为 2 分钟，并定期发送等待心跳。
- 收到 `finish_reason` 后即使上游不发送 `[DONE]`，也会在宽限期后结束。
- Shell 命令默认 120 秒超时，可由运行设置调整。
- 所有 Agent 工具统一经过宿主级执行看门狗；即使工具忽略 Context，也会按配置的 `toolTimeoutSeconds` 返回超时终态。
- 任务级空闲看门狗覆盖整轮模型、审批、工具、MCP、插件、子代理和团队等待，活动事件会续期，超时由 `taskIdleTimeoutSeconds` 配置。
- 运行状态统一包含 `running`、`waiting`、`retrying`、`failed`、`cancelled` 和 `completed`，前端对等待、重试和失败使用不同呈现。
- 超时或取消后的迟到工具结果和进度事件会被丢弃，不再写回当前会话状态。
- 停止操作立即取消任务 Context；2 秒内未退出时，前端收到强制 `cancelled` 终态。
- Windows Shell 子进程使用 Job Object，正常取消时终止进程树。

### 未完成

- [x] 为所有工具增加统一的执行超时包装，不能只依赖工具自行遵守 Context。
- [x] 增加任务级空闲看门狗，覆盖模型、审批、工具、MCP、插件、子代理和团队角色。
- [x] 统一状态枚举：`running`、`waiting`、`retrying`、`failed`、`cancelled`、`completed`。
- [x] 强制终态后隔离仍未退出的工具，不能继续写入当前会话或覆盖新任务状态。
- [x] 失败无法推进时持久化终态、错误、已完成结果和恢复入口。
- [x] 增加“工具忽略 Context”和“取消后迟到进度”回归测试。
- [x] 增加“MCP 永不返回”“插件忽略 Context”“审批等待取消”和“停止时子进程有后代”等回归测试。
- [x] 增加应用级 `StopChatMessage` E2E：模拟模型调用真实 `run_command` 并派生子进程，验证停止立即返回、Windows Job Object 清理进程树、只持久化一个命令终态和一个 `cancelled` 会话终态，且取消后不再请求模型。

### 验收标准

停止按钮触发后 200 毫秒内 UI 进入停止中，2 秒内必须出现唯一终态；任何单个工具都不能让会话永久保持运行中。超时或取消后的迟到结果不得污染会话。

## P0-2：重复失败与策略切换

### 已实现

- 对工具名、归一化参数、退出码和错误类别生成等价失败签名。
- 忽略引号、反斜杠和空白差异，识别只改引号的等价失败。
- 检测相同结果循环和交替循环。
- 确定性失败发生一次后，第二次等价调用会在执行前被拦截；暂时性错误只允许一次受控等价重试。
- 失败状态只持久化参数哈希、退出码、错误类别和替代工具，不保存原始命令或凭据。
- 会话重启、切换、上下文压缩和 rewind 后恢复当前分支的失败策略状态。
- 连续忽略诊断并重复被拦截的方案会关闭本轮工具调用，要求模型改用不同策略或明确报告阻塞。
- 不再使用固定工具调用次数作为长任务终止条件。

### 未完成

- [x] 将失败指纹、策略类别和已尝试替代方案保存到会话分支状态。
- [x] 第二次等价失败前就向模型提供结构化诊断，不允许无实质变化的重试落地。
- [x] Shell 文件或 Office 操作失败后，明确推荐对应的结构化工具。
- [x] 区分暂时性错误、参数错误、权限错误、工具缺失和确定性数据错误。
- [x] 为停滞后的换工具、换协议、降级验证和最终失败增加 E2E。

## P0-3：Windows Shell 参数与结构化进程启动

### 已实现

- [x] `run_command` 增加独立的结构化进程模式：`executable`、`args[]`、可选 `working_directory`。
- [x] 字符串 Shell 模式与结构化进程模式互斥，分别校验和显示。
- [x] 结构化模式使用 `exec.CommandContext(executable, args...)`，不得再次经过 PowerShell/CMD 拼接。
- [x] 为 `python -c`、中文路径、空格、换行、单双引号、反斜杠和 Unicode 输出增加 Windows 回归测试。
- [x] 确认取消和超时时结构化进程同样进入 Job Object 并终止后代。
- [x] 调整工具说明：文本、目录、搜索、Office 和浏览器操作优先使用结构化工具。

### 待验证

- [x] 运行全量 Go 测试、协议测试、静态检查、前端构建和 Wails 生产构建。
- [ ] 在发布版中执行一次真实长命令停止测试，确认任务时间线、进程树和唯一终态一致。

### 验收标准

同一段 Python 代码通过结构化参数执行时，参数必须逐字节到达 Python；不得出现宿主额外添加引号导致的 `unterminated string literal`。

## P0-4：项目身份与重新挂载

### 已完成

- 移除项目只解除侧边栏挂载，并保存到 `DetachedProjects`。
- 使用规范化绝对路径、符号链接解析和 Windows 大小写不敏感比较识别同一项目。
- 重新添加同一路径恢复原项目 ID、会话 ID 和历史事件目录。
- 项目与会话定位使用组合身份，避免多个项目的“新对话”互相覆盖。
- 每个项目持久化 `lastActiveSessionId`；项目切换、会话切换、新建会话、归档和删除时同步维护，旧清单会在启动时自动补齐活动项目字段。
- 重新挂载不再按更新时间猜测会话；即使另一个会话更新得更晚，也会恢复用户移除项目之前最后选中的会话。
- 解除挂载期间重启后，重新挂载仍恢复原事件 HEAD、两条 rewind 分支、checkpoint、运行中计划、暂停中的 AI 团队 checkpoint、项目长期记忆、供应商会话身份和稳定前缀哈希。

### 后续验证

- [x] 补充分支、checkpoint、长期记忆和团队中断状态的重新挂载 E2E。
- [ ] 使用真实 DeepSeek/OpenAI 兼容上游测量重新挂载前后的缓存前缀稳定性。
- [ ] 若增加“永久删除项目数据”，必须单独设计危险确认，不复用移除操作。

### 2026-07-27 验证记录

- 底层项目清单测试覆盖“多会话项目解除挂载 → 应用重启 → 同路径重新挂载”，确认恢复最后活动会话而不是最近更新会话。
- Agent E2E 覆盖项目记忆会话、双分支及多 checkpoint、运行中计划、暂停团队、解除挂载期间重启和重新挂载；恢复前后的项目/会话 ID、HEAD、分支叶子、消息事件 ID、项目记忆摘要、供应商会话 ID 和稳定前缀哈希一致。
- `go test ./... -count=1`、`go vet ./...`、`git diff --check`、79 项前端测试、TypeScript 检查、Vite 生产构建和 `wails build -clean` 均通过。

## P0-5：会话级产物登记表

### 已完成

- [x] 新增 append-only `artifact_update` 事件；产物登记与对话、文件快照共用 ParentID 事件树，不维护第二套可变数据库。
- [x] 工具成功返回 `PartFile` 或 `FileChange` 后立即登记，不等待最终模型回答；任务中途停止但保留结果时不会丢失路径。
- [x] `ArtifactRecord` 保存规范化绝对路径、工作区显示路径、文件名、类型、MIME、大小、mtime、SHA-256、动作和状态。
- [x] 保存工具、工具调用 ID、所属用户消息、项目和会话；分支叶子与下一 checkpoint 在读取当前事件链时重新派生。
- [x] 保存结构验证、视觉验证、失败原因、预览/渲染引用和最后检查时间；Office 文件重新打开失败时标记 `invalid/failed`，视觉验收尚未执行时明确标记 `pending`。
- [x] 支持 `created`、`modified`、`available` 和 `deleted`；删除后 rewind 会同时恢复文件与之前的产物记录。
- [x] rewind 和分叉只读取当前 ParentID 链，不会把被回退分支的未来产物注入模型或前端状态。
- [x] 应用重启、项目会话切换和上下文压缩后，从登记事件直接生成模型私有产物上下文；旧版消息中的路径片段仍可兼容读取，不会遍历整个工作区猜测文件。
- [x] `WorkbenchState.artifacts` 暴露当前会话分支登记表，供后续右侧产物界面和视觉验证流程直接使用。

### 验证

- [x] 覆盖立即回喂模型、重复登记去重、重启、会话隔离与切回、rewind、分叉切换、删除恢复、损坏 XLSX 和结构化上下文往返。
- [x] `go test ./... -count=1`、`go vet ./...`、76 个前端测试、TypeScript 检查和 Vite 生产构建通过。
- [ ] 当前 Windows Go 环境未启用 CGO，`go test -race` 无法运行；并发子代理登记已使用独立互斥和原始 tool-call ID 去重，仍应在 CI 的 CGO race job 中复验。

## P0-6：Anthropic 新模型能力与真实上下文长度

### 已确认问题

- `claude-fable-5` 与 `claude-opus-5` 通过 Anthropic 原生协议请求时返回：`temperature is deprecated for this model`（HTTP 400）。当前请求组装仍无条件发送 `temperature`，导致模型完全不可用。
- 模型设置页将 `claude-fable-5`、`claude-opus-5` 及多个新模型统一显示为 `200000 / 协议默认`。这只是回退值，不代表该模型的官方真实上下文窗口。
- 不能仅按供应商或协议统一决定参数；同一 Anthropic 端点内，不同模型可能支持不同的采样、推理和输出参数。
- 当前 Anthropic 原生请求把全局推理档位同时映射成 `thinking.type`、`thinking.budget_tokens` 和 `output_config.effort`。不同代际模型可能分别要求手动 thinking budget、自适应 thinking、特定 effort 枚举或完全不接受其中某些字段；未经模型能力判断直接组合发送，会造成推理强度没有生效或直接 HTTP 400。
- 自定义 Anthropic 中转也不能仅凭供应商名称判断字段。必须结合规范化模型 ID、上游端点能力和明确的兼容错误决定请求体，且不能把 OpenAI 的 `reasoning_effort` 值原样套入 Anthropic 原生协议。

### 已实现

- [x] 新增 Anthropic 独立能力矩阵，区分不支持、手动 `budget_tokens` 和 adaptive thinking，并记录是否允许关闭、最大 effort、采样限制、最大输出及已确认上下文窗口。
- [x] 支持 `claude-fable-5`、`claude-mythos-5`、`claude-mythos-preview`、`claude-opus-5`、`claude-sonnet-5`、Opus 4.6-4.8、Sonnet 4.6 及旧 4.x/3.7 精确别名和八位日期版本。
- [x] Claude 5 保留真实 `xhigh` / `max`；Fable/Mythos 不再发送非法 `disabled`，Opus 5 的 `disabled + xhigh/max` 冲突会归一化为 adaptive。
- [x] 对明确废弃采样参数的模型完全省略 `temperature`；旧模型在 thinking 已关闭且仍支持采样时保留用户温度。
- [x] Claude 5 和已确认的新 4.x 使用 128K `max_tokens`；手动 thinking 预算最多占最终 `max_tokens` 的 75%，不足以容纳最小 1024 token 预算时省略 thinking，保证请求不会违反 `budget_tokens < max_tokens`。
- [x] Anthropic Models API 的 `max_input_tokens`、`max_tokens`、effort 档位和 thinking 类型会保存到模型配置，并随主 Agent、AI 团队、子代理及故障切换路由传递。
- [x] 自定义模型别名优先使用上游返回的 effort/thinking 能力；不支持当前档位时降到最近合法档位，而不是套用 OpenAI 字段。
- [x] 上下文来源优先级保持为手动值、上游元数据、精确目录、供应商默认和安全估算；未知 Anthropic 模型不再统一伪报 200K。
- [x] 后端和前端同步 Claude 精确目录，并覆盖原生 Complete/Stream JSON、日期别名、旧模型、自定义别名、模型能力持久化和未知上下文回退测试。

### 待验收与补强

- [x] 通过 Anthropic 官方 SDK/OpenAPI 生成类型核对 `anthropic-version: 2023-06-01`、`adaptive/enabled/disabled` thinking、`budget_tokens >= 1024` 且小于 `max_tokens`、`output_config.effort` 以及 Models API 能力字段。
- [x] 新 Claude 5 不再静态写死 1M 上下文；优先采用 Models API 返回的 `max_input_tokens`，没有真实上游值时明确使用 64K 安全估算并保留来源。
- [ ] 在真实官方端点核对工具调用及 `context-1m-2025-08-07`、`interleaved-thinking-2025-05-14`、`output-128k-2025-02-19` beta header 的启用条件，未验收前不得凭模型名字自动发送 beta。
- [x] 对自定义/中转模型保留安全兼容路径：先使用已知能力；若上游明确返回参数不支持，只移除该参数重试一次，并持久化本端点模型的能力结果，不能把 EOF、超时或其他 400 当成参数兼容问题。
- [ ] 使用官方 Anthropic 与至少一个真实 Anthropic-compatible 中转分别完成普通对话、原生工具循环、`none/xhigh/max` 和 1M 上下文元数据验收。

### 2026-07-27 兼容性学习验证

- `temperature`、`thinking` 与 `output_config/effort` 只有在 HTTP 400/422 明确声明不支持、废弃或不允许时才进入兼容路径。
- 首次请求只重试一次；EOF、超时、服务端 5xx、普通参数错误和未识别字段不触发兼容重试。
- 学习结果按供应商 ID 与模型 ID 写入 `runtime-settings.json`，并由主 Agent、AI 团队、子代理和故障切换路由带入后续请求。
- 同一运行任务还会共享内存能力缓存；工具循环、AI 团队和并行子代理无需等到下一轮或重启，就会立即省略已拒绝字段。
- 已学习字段在下一轮首个请求中直接省略，不再重复撞同一个上游错误。
- 手动 thinking 在最终请求组装阶段受实际 `max_tokens` 约束；4K 输出上限会把预算收缩到 3K，1.2K 输出上限会省略无法合法容纳的 thinking。
- 协议与 Agent 聚焦测试、TypeScript 检查均已通过；官方端点和真实中转验收仍需用户配置对应 API Key 后执行。
- 最终回归通过 `go test ./... -count=1`、`go vet ./...`、`git diff --check`、79 项前端测试、TypeScript 检查、Vite 生产构建和 `wails build -clean`；新的 Windows amd64 二进制已生成到 `build/bin/MHcode.exe`。

### 验收标准

上述模型能够完成普通对话和工具调用，请求 JSON 中不出现已废弃的 `temperature`，推理档位在上游请求记录中能看到对应且合法的 Anthropic 原生字段；设置页显示的上下文长度必须带可追溯来源，未知值不得统一冒充 200K。

## P0-7：本机诊断路由与工具结果最终综合

### 已确认问题

用户要求检查本机 Claude Desktop 与 CCSwitch 的供应商/模型配置是否串用。MHcode 实际只执行了 `web_search`，随后输出一组 CCSwitch 搜索结果，没有检查本机配置、进程环境或当前生效端点，也没有给出诊断结论。

截图中的“网络搜索已完成。以下摘要直接整理自搜索来源”不是模型最终回答，而是宿主代码在模型工具调用后返回空正文时，通过 `emptyToolCompletionContent` 和 `webSearchFallbackContent` 生成的兜底文本。当前逻辑把“有搜索来源”错误等同于“任务已完成”，因此这是 Agent 编排与终态判定问题，不能归因于模型不够聪明。

### 正确执行流程

1. 识别任务主体是“检查我的本机配置”，网络查询只是辅助，不是最终目标。
2. 先确认用户指的是 Claude Desktop、Claude Code 还是两者，并读取 CCSwitch 当前选中的供应商、它实际写入的目标配置和生效范围。
3. 在用户授权后，用结构化文件/电脑工具检查相关配置文件、环境变量、进程启动环境和客户端缓存；不得遍历整盘，也不得显示 API Key 明文。
4. 比较“空白群友”和“薄荷中转”的 Base URL、模型映射与当前运行进程实际使用值，判断是写错配置目标、旧进程未重启、环境变量覆盖，还是客户端读取了另一套配置。
5. 只有不知道 CCSwitch 配置位置或版本行为时才查网络；搜索后必须打开并读取官方文档或真实仓库页面，不能把搜索摘要当证据。
6. 最终输出已检查位置、实际生效来源、冲突项、结论和可执行修复步骤；若权限不足，应明确请求需要的本机读取权限，不能静默改成泛泛搜索。

### 已实现

- [x] 本机配置类请求在稳定提示中优先使用授权的文件、电脑和进程状态工具，搜索只能补充官方资料。
- [x] 搜索结果只作为来源发现；模型必须继续读取官网、官方文档、真实仓库或本机状态后才能形成成功答案。
- [x] 工具轮正文为空时保留已有工具结果并继续调查；再次为空时进入禁用新工具的最终综合。
- [x] 最终综合仍为空时返回明确失败，不再由宿主生成“网络搜索已完成”的伪成功正文。
- [x] 旧会话中的搜索兜底迁移为“本轮未完成最终分析”，不会在恢复会话时继续冒充已完成答案。
- [x] 已覆盖搜索后继续读取、本机 Claude/CCSwitch 诊断、最终综合、二次空正文失败和 rewind 恢复测试。

### 验收标准

面对 Claude Desktop/CCSwitch 配置串用问题，Agent 必须检查或请求检查本机真实状态，并给出证据化结论；只返回网络搜索结果列表永远不能算任务完成。

### 2026-07-27 验证记录

- `go test ./internal/agent -count=1` 已通过；搜索摘要不再成为成功答案，二次空正文会进入失败终态。

## P1-1：通用视觉验证闭环

### 已实现

- [x] 定义统一的 `render_artifact` 和 `inspect_visual` 工具；渲染与视觉通过是两个独立状态。
- [x] 图片使用有界 PNG 渲染；HTML、PDF 和解析后的 DOCX/XLSX/PPTX 通过隔离的本机回环页面截图；当前浏览器标签、已授权桌面窗口和 MHcode 自身窗口使用真实画面截图。
- [x] 回环预览使用不可猜测令牌、短生命周期服务和项目根目录校验；HTML 相对资源不能通过路径穿越或符号链接读取项目外文件，也不会创建、替换或覆盖用户可见标签页。
- [x] 渲染文件只写入 MHcode 私有缓存，不修改用户项目；窗口和浏览器截图也会限制到请求尺寸。
- [x] 每次文件渲染绑定源文件 SHA-256；文件修改后旧渲染立即失效，必须重新渲染。渲染引用与视觉状态写入 append-only 产物事件，重启后可按路径恢复。
- [x] 渲染图片作为独立附件交给已配置的图像模型；视觉检查只接受严格 JSON，输出 `passed` 或 `changes_required` 及结构化严重级别、位置、问题和修正建议。
- [x] `changes_required` 会把产物标记为失败并要求修改、重新渲染和重新检查；只有当前 SHA 的 `passed` 可以作为视觉通过依据。
- [x] 已知纯文本模型不会收到图片；视觉路由不可用、返回格式错误或全部失败时，明确降级为 `degraded` 结构检查，绝不伪装成视觉通过。
- [x] 模型准备结束但本轮视觉产物仍是 `pending`、`rendered` 或 `failed` 时，宿主只追加一次视觉验收请求；再次忽略则结束任务并在最终回答中明确标记“视觉验收未完成”，不会形成无限工具循环。
- [x] 前端时间线将渲染与视觉检查显示为独立活动，等待和重试期间保持实时运行状态。
- [x] Office 视觉面使用 MHcode 结构解析预览，不冒充 Microsoft Office 原生像素或原生排版保真度。

### 自动化验证

- [x] 覆盖工具 schema 与路径权限、HTML 转义和工作表/幻灯片选择、PNG 尺寸限制、回环令牌和符号链接逃逸。
- [x] 覆盖视觉通过、要求修改、格式错误、已知纯文本模型降级、SHA 失效、重启恢复、单次最终门禁和完整“渲染 → 检查 → 结束”循环。
- [x] 本机设置 `MHCODE_BROWSER_RENDER_PROBE=1` 后，真实 Edge/Chrome 后台渲染探针通过：生成 800×600 非空 PNG，且渲染前后未进入用户可见标签列表。
- [x] 本机设置 `MHCODE_ARTIFACT_RENDER_ACCEPTANCE=1` 后，DOCX、XLSX、PPTX、HTML 和 PDF 均通过真实 Chromium 后台渲染：输出 960×720 非空 PNG、源文件 SHA-256 前后相同、用户标签列表和活动标签始终为空。
- [x] 使用 `MHCODE_ARTIFACT_ACCEPTANCE_EXISTING_XLSX` 对现有 `员工月度考勤表.xlsx` 完成同一套只读渲染验收，确认带中文路径的真实工作簿不会被预览过程修改。
- [x] `go test ./... -count=1`、`go vet ./...` 与 `git diff --check` 通过。
- [x] 前端 79 项测试、TypeScript 检查和 Vite 生产构建通过。
- [x] `wails build -clean` 通过，生成 Windows amd64 生产二进制 `build/bin/MHcode.exe`。

### 待人工验收

- [ ] 在 Windows 发布版从 UI 分别渲染 HTML、PDF、DOCX、XLSX、PPTX，并继续验收当前网页、MHcode UI 和另一个已授权窗口，确认不会闪出新标签、覆盖用户面板或改变当前侧栏。
- [ ] 使用至少一个真实 OpenAI-compatible、Anthropic 和 Gemini 视觉模型验证图片字段、严格 JSON、上游失败切换与费用记录；确认跨供应商视觉路由符合用户配置和数据边界。
- [ ] 让真实模型对一个有明显裁切问题的表格或网页完成“发现问题 → 修改 → 新 SHA 重渲染 → 通过”的完整迭代，并确认时间线、停止、切换会话和重启恢复正常。
- [ ] 使用 Microsoft Office 或兼容办公软件打开生成文件做最终保真验收；MHcode 解析预览通过不能替代官方软件打开验证。

## P1-2：Codex 式实时工作时间线

### 已实现

- 工具开始、实时进度、完成和失败事件。
- Shell 命令、工作目录、stdout、stderr、退出码和耗时。
- 工具详情默认折叠，用户展开状态跟随当前会话，不因新输出自动收起。
- 计划、子代理和 AI 团队状态进入对话事件通道。
- [x] 等待、重试、停止中、失败和恢复均进入结构化任务/工具事件，不再只依赖临时状态文字。
- [x] 每个会话使用原子覆盖的 `task-runtime.json` 保存当前任务快照；文本、推理、模型、工具、计划、团队、子代理、时间戳和累计耗时均可恢复，流式增量按 750 毫秒节流写入，不使 `events.jsonl` 无限增长。
- [x] 快照锚定当前事件分支；rewind 或切换分支不会恢复其他分支的运行状态。
- [x] 启动时把没有终态的旧快照恢复为持久化 `interrupted` 回合，同时收束仍在运行的工具、计划、团队和子代理；重复启动不会重复追加消息。
- [x] 若 Assistant/checkpoint 已落盘但最终快照尚未来得及更新，恢复逻辑会识别已提交回合并标记完成，不会错误生成中断消息。
- [x] 活跃任务接口返回完整运行快照；WebView 刷新和切换会话后可恢复流式正文、推理、进展文字、工具记录、累计用时和当前状态。
- [x] Agent 在连接上游、规划、上下文压缩、等待、重试和最终综合等可核验节点写入 `timeline_note`；它们不计作模型有效正文，因此不会破坏失败时乐观消息回滚规则。
- [x] 自然语言进展与工具动作一起持久化到最终消息；历史任务和异常中断任务均可回显“上次运行中断”。
- [x] 覆盖实时快照、终态收束、崩溃恢复幂等、已提交回合识别、跨会话恢复、展开状态保持及自然语言进展的 Go/前端回归测试。

### 本轮验证

- [x] `go test ./... -count=1`：全部通过，之前偶发的终端 Unicode 用例本轮未复现。
- [x] `go vet ./...`：通过。
- [x] `frontend/bun.cmd run check`：77 项前端测试、TypeScript 和 Vite 生产构建通过。
- [x] `wails build -clean`：Windows amd64 生产构建通过，生成 `build/bin/MHcode.exe`。
- [x] 浏览器检查深色、浅色及 820/520 像素窄窗口：无横向溢出和控制台错误；临时视口已恢复。

### 待人工验收

- [ ] 在发布版中运行一次包含模型流、Shell 实时输出、等待/重试、计划及子代理的真实长任务，确认时间线的视觉密度和滚动体验。
- [ ] 长任务运行中频繁切换两个项目会话，确认各自正文、累计用时、展开状态和运行标签不串线、不闪空。
- [ ] 在任务运行中强制结束 MHcode 并重新启动，确认只出现一个 `interrupted` 终态，发送“继续”后能从已保存计划和产物继续。

## P1-3：Agent 上下文与编排审计

- [x] 审计 System Prompt，移除互相冲突或过度限制模型的规则。
- [x] 检查项目、附件、产物、失败原因、计划和未完成任务的注入顺序；修复 `VolatileTail` 只在 UI 预览、未进入真实模型请求的问题。
- [x] 将触发 Skill 正文、项目长期记忆、动态授权、执行状态和输出要求作为不可见的轮次上下文紧邻用户消息追加；用户原文不重复，后续请求仍保持追加式缓存前缀。
- [x] 压缩后保留当前分支、未完成步骤、最近失败策略、最新执行 checkpoint 和完整产物索引；旧动态上下文不会混入会话摘要。
- [x] 工具结果向模型保留结构化错误、完整退出码、工作目录和必要 stdout/stderr，长内容受边界完整的私有块预算约束。
- [x] 停止后输入“继续”能够恢复计划、产物、团队 checkpoint、中断位置和最近工具终态；切换会话与重启后由 append-only 事件中的结构化 parts 重建。
- [x] 覆盖普通 Agent、Plan、AI 团队、并行子代理和 MCP 的长任务 E2E。

### 本轮验证

- [x] DeepSeek/OpenAI-compatible 两轮请求保持完整公共消息前缀，私有动态上下文不重复用户输入。
- [x] 普通 Agent、Plan、AI 团队角色和并行子代理均继承同一份私有动态上下文。
- [x] 压缩只保留最新轮次上下文和最新执行 checkpoint，边界标记不会被头尾裁剪破坏。
- [x] 产物登记与执行 checkpoint 在存在新版 `artifact_update` 事件时仍能同时从会话切换和应用重启中恢复。
- [x] Streamable HTTP MCP 已覆盖发现、稳定 schema、真实 Agent 工具调用、结果回喂和私有上下文保持；只读 MCP 可进入 Plan/探索注册表，写入型 MCP 不会泄漏到只读阶段。
- [x] 超过旧固定工具上限的长任务、停止后保留进展、服务重启后发送“继续”复用执行 checkpoint 与产物路径均已 E2E；不会重复执行已成功的写入。
- [x] 首轮模型错误只有在明确拒绝 `tools`/function-calling 字段时才降级为普通对话；EOF、deadline、安全策略和无关 400 保留真实错误。
- [x] System Prompt 不再强制模型简短、固定结构化摘要或在每个细小动作后更新计划；私有轮次上下文与用户原文的顺序已明确，用户授权的外部绝对路径可进入结构化工具。

### 2026-07-27 Skill 注入专项修复

- [x] 删除 `mhcode-agent-core` 对 `agent`、`plan`、`缓存`、`协议`、`MCP`、`tokens` 和 `skill` 等通用词的特殊宽泛匹配；Skill 名称不再拆成 `agent/core/helper` 等碎片触发。
- [x] Skill 运行时支持 `explicit`、`manual` 和兼容旧 Skill 的 `description` 三种触发模式；完整 Skill 名称始终可以显式调用，ASCII 触发词按单词边界匹配。
- [x] MHcode 专属触发配置迁入 `agents/mhcode.yaml`，保持标准 `SKILL.md` frontmatter 仅包含 `name` 和 `description`。
- [x] `mhcode-agent-core/SKILL.md` 从约 3882 token 精简至约 631 token，内部流程迁入 `docs/agent-internal-design.zh-CN.md`；Office 产物规则拆为独立 `mhcode-office-artifacts` Skill。
- [x] 设置页上下文诊断显示本轮触发 Skill 名称、注入字符数和估算 token；普通请求应显示 0，方便直接定位误触发和上下文膨胀。
- [x] 仓库级回归确认普通“给 Agent 协议解析增加缓存并制定 Plan”不会触发任何 Skill；显式“修改 MHcode Agent 的上下文组装”只触发核心 Skill；创建 `.xlsx` 只触发 Office Skill。

### Skill A/B 验收边界

- A 组使用修复前规则，B 组使用当前显式触发规则；两组必须使用同一供应商、模型、推理档位、项目快照、会话历史和任务集。
- 任务集至少包含普通编码、Agent/Plan/缓存等通用技术词、MHcode 内核开发和 Office 产物四类，每类不少于 10 个固定任务。
- 记录每轮触发 Skill、Skill 注入 token、总输入 token、首个有效工具选择、无效/重复工具调用、完成率、用户纠正次数、耗时和上游缓存命中 token。
- 自动化硬门槛：普通任务误触发率必须为 0；显式 MHcode 与 Office 任务正确触发率必须为 100%；核心 Skill 单次估算注入不得超过 900 token。
- 线上质量门槛：B 组任务完成率不得低于 A 组，无效工具调用和用户纠正次数应下降；缓存命中率只在相同稳定前缀与至少 30 轮样本下比较，不能用短样本百分比代替质量结论。
- [ ] 使用至少一个真实 OpenAI-compatible 或 DeepSeek 上游完成上述固定任务集 A/B；自动化测试已证明触发边界，但尚不能替代真实模型行为和缓存账单验收。

### 完成验证

- [x] `go test ./... -count=1`
- [x] `go vet ./...`
- [x] `frontend/bun.cmd run check`：79 项前端测试、TypeScript 和 Vite 生产构建通过。
- [x] `wails build -clean`：Windows amd64 生产构建通过，生成 `build/bin/MHcode.exe`。

## P1-4：Agent 结构化拉取与下载

### 已实现

- [x] `download_file` 和 `git_repository` 已进入主 Agent、团队实现者和实现型子代理的可变工具表；Plan 只读阶段不暴露落盘工具。
- [x] HTTP/HTTPS 下载支持工作区、额外可写目录和用户本轮明确授权的绝对路径；宿主仍以规范化路径和临时可写根执行最终权限校验。
- [x] 下载提供实时字节数、总大小、速度、取消、有限重试、重定向、最大尺寸、临时文件和原子落盘；失败、校验不符和取消都会清理不完整文件。
- [x] 支持 SHA-256、`Content-Disposition` 文件名、`error/overwrite/rename` 同名策略；响应头和真实文件头会共同识别伪装 HTML。
- [x] URL 用户信息、查询参数和短时签名不会进入审批、实时进度、Git stdout/stderr 或最终时间线；动态改名后的实时与最终记录按 `toolCallId` 合并。
- [x] 下载成功立即产生文件片段并登记会话级产物，规范化绝对路径、哈希、大小、类型和工具调用归属可在后续轮次、会话切换及重启后恢复。
- [x] 稳定 Agent 规则要求先读取厂商官网，识别系统、CPU 架构、发布通道、版本和安装包格式，再将最终 HTTP(S) 资源交给 `download_file`；搜索摘要和产品 HTML 不能冒充安装包。
- [x] 官网下载任务允许受控浏览器参与 JavaScript 页面定位；普通搜索仍不能擅自打开网页，用户明确禁止浏览器时优先服从禁止要求。
- [x] 下载与执行安装包是两个独立动作；下载成功不会隐式授权执行，后续安装仍经过运行权限和审批。
- [x] `git_repository` 使用结构化参数直接启动 Git，支持 clone/fetch/pull、浅克隆、分支、prune 和 `ff-only/rebase/merge`；不依赖 Shell 权限，clone 失败或取消会清理临时仓库。
- [x] Git 拉取实时显示命令、工作目录、stdout、stderr、退出码和耗时；前端使用可折叠 Git 命令详情卡。
- [x] clone 遇到 TLS 握手、连接重置和临时网络错误时最多重试两次，每次使用新的临时目录；认证失败、无权限和仓库不存在不会盲目重试，fetch/pull 也不会自动重复副作用。
- [x] 显式启用 `MHCODE_EXTERNAL_DRIVE_ACCEPTANCE=1` 后，D/E/F 三个真实盘符均在中文和空格目录完成 GitHub README 下载、MHcode 浅克隆、`pull --ff-only` 及临时目录清理。

### 仍需人工验收

- [ ] 在发布版上选择数个真实软件官网，覆盖静态直链、JavaScript 按钮、多级跳转、厂商 CDN 和短时签名 URL，确认模型选择正确的 Windows 架构与稳定版资源。
- [ ] 使用真实大文件覆盖慢速下载、中途断网、重试、停止、同名冲突和 SHA-256/数字签名核验；确认 UI 长时间刷新稳定且不会留下半成品。
- [ ] 重启发布版后继续引用跨盘下载和仓库产物，并确认下载完成后不会未经用户确认启动 EXE、MSI 或脚本。

### 验收标准

用户可以说“去这个软件官网下载适合我电脑的最新版到 D 盘”“把这个文件下载到指定目录”或“把这个仓库拉取到指定目录”。Agent 会核验来源和版本、展示实时动作并生成真实可用产物；权限、失败清理、停止和会话恢复均可验证，且不会未经确认直接运行安装包。

## 每轮开发要求

1. 开始前读取本文档并选择最靠前的未完成 P0。
2. 修改状态前必须具备对应代码、测试和异常路径证据。
3. 每完成一个子项，立即更新本文档，不在任务末尾批量伪造完成状态。
4. 至少运行聚焦测试；共享 Agent 链路变化后运行 `go test ./... -count=1`、`go vet ./...`、前端检查和生产构建。
5. 未完成的功能必须明确写“进行中”或“未开始”，不得使用“基本完成”掩盖缺口。

## 下一步入口

下一阶段先完成全量回归和生产构建，再对 **P1-1：通用视觉验证闭环**、**P1-2：实时工作时间线** 和 **P1-4：结构化拉取与下载** 做发布版 UI 人工验收；**P0-5** 仍需真实上游缓存重新挂载测试，**P0-6** 仍需官方 Anthropic 与真实中转环境核验。当前机器未发现 Microsoft Office 或兼容办公软件可执行文件，因此原生打开保真度仍是明确的人工验收项。
